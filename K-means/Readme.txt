Intructions to run the code:

Note: Keep all the .m files and images provided in the same folder.

Go to main.m and change the image name to be used for the algorithm.
Run main.m to run the algorithm.
On running, a dialogue box will appear with all the instruction to select mask.
Once mask selection is over, algorithm will start running with a window showing each update.

For more details of the code see implementation section of the report.

|-------------------------------------------------------| END |--------------------------------------------------------------|